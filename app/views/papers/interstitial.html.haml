- content_for :title do
  Redirect to #{@ref[:info][:title]}
  
%h3 Originating page
%p
  = render partial: "cite", object: @paper.info[:paper], locals: {cite_href: "/view/#{URI.encode_www_form_component(@doi)}"}

  
%h3 Destination
%p
  = render partial: "cite", object: @ref[:info], locals: {cite_href: "http://dx.doi.org/#{URI.encode_www_form_component(@ref[:info][:DOI])}"}

%p
  Redirecting ...

#redirect
  :javascript
    redirectOnce("http://dx.doi.org/#{URI.encode_www_form_component(@ref[:info][:DOI])}");
